# 🚀 Mawqūtan 10/10 — V3 Implementation Plan

> 36 tasks × 2–5 min. Based on actual code audit (App.tsx, prayerTimes.ts, store).
> Corrected: Linear spacing (4-8-16-24-32), single radius (6px), no API cache.

**Goal:** 7/10 → 10/10. WCAG AA, UX flow, design tokens, polish.

**Architecture:** No new deps. Pure CSS vars + React state. Offline-native (adhan computes locally).

**Reference:** Linear dark theme — single accent, 4px spacing grid, 6px radius, flat surfaces.

---

## 🎯 Step 0: Excalidraw Wireframe (Pick Your Design)

Before any code, let's visualize the options. I'll generate an Excalidraw JSON of the prayer screen showing **3 variants for Settings button position** and **2 variants for countdown ring**. You pick, then we execute.

**(Say "Excalidraw" and I'll draw it)**

---

## Phase 1: ♿ Accessibility & Contrast (8 tasks ~25 min)

### Task 1.1 — Hijri date → #a098c0
**File:** `src/App.css:401-407`
```patch
- color: #6b6390;
+ color: #a098c0;
```
**Verify:** `grep '#6b6390' src/App.css` → 0 matches.
**Commit:** `fix(a11y): hijri date #6b6390→#a098c0`

### Task 1.2 — Verse/footer → #6b6380 (was #3a3450)
**File:** `src/App.css:320` (`.region-verse`), `src/App.css:500` (`.prayer-footer`)
Do twice. `grep '#3a3450'` → 0.
**Commit:** `fix(a11y): verse text #3a3450→#6b6380`

### Task 1.3 — Placeholder → #7a7390 (was #5a5470)
**File:** `src/App.css:265,288`
Do twice. `grep '#5a5470'` → 0.
**Commit:** `fix(a11y): placeholder #5a5470→#7a7390`

### Task 1.4 — Past rows opacity 0.3 → 0.4
**File:** `src/App.css:469`
**Commit:** `fix(a11y): past prayer rows 0.3→0.4`

### Task 1.5 — prefers-reduced-motion guard
**File:** `src/App.css` — append (line 665+):
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```
**Commit:** `fix(a11y): prefers-reduced-motion support`

### Task 1.6 — ::selection
**File:** `src/App.css:72` — after body block:
```css
::selection { background: rgba(212, 168, 67, 0.3); color: #e8e0d0; }
```
**Commit:** `feat: custom ::selection colors`

### Task 1.7 — :focus-visible
**File:** `src/App.css` — after ::selection:
```css
*:focus-visible { outline: 2px solid #d4a843; outline-offset: 2px; }
```
**Commit:** `fix(a11y): focus-visible outlines`

### Task 1.8 — Build + verify
`npm run build` → clean. `git add src/App.css && git commit -m "phase 1: accessibility complete"`

---

## Phase 2: 🧭 UX Flow (10 tasks ~50 min)

### Task 2.1 — showSettings store state
**File:** `src/stores/useStore.ts:32-38` — add to AppState:
```ts
showSettings: boolean;
setShowSettings: (v: boolean) => void;
```
In store (line 58+):
```ts
showSettings: false,
setShowSettings: (v) => set({ showSettings: v }),
```
**Commit:** `feat: add showSettings store state`

### Task 2.2 — Settings button in prayer header
**File:** `src/screens/PrayerTimes.tsx:81-87` — add inside header div:
```tsx
<button className="prayer-settings" onClick={() => useStore.getState().setShowSettings(true)}
  title="الإعدادات"><svg width="16" height="16" viewBox="0 0 16 16" fill="none">
  <circle cx="8" cy="8" r="3" stroke="currentColor" strokeWidth="1.2"/>
  <path d="M8 1v2M8 13v2M1 8h2M13 8h2" stroke="currentColor" strokeWidth="1.2" strokeLinecap="round"/>
  <path d="M2.5 2.5l1.5 1.5M12 12l1.5 1.5M2.5 13.5l1.5-1.5M12 4l1.5-1.5"
    stroke="currentColor" strokeWidth="1.2" strokeLinecap="round"/></svg></button>
```
**File:** `src/App.css` — add:
```css
.prayer-settings {
  position: absolute; left: 0; top: 50%; transform: translateY(-50%);
  background: none; border: none; color: #8b83a0; cursor: pointer;
  padding: 4px; border-radius: 4px; transition: color 0.15s;
}
.prayer-settings:hover { color: #d4a843; }
```
**Commit:** `feat: settings button in prayer header`

### Task 2.3 — Route to RegionSelect (based on actual App.tsx:92-94)
**File:** `src/App.tsx:92-94` — change:
```tsx
{(!region || showSettings) && <RegionSelect />}
{region && timer.active && <TimerScreen />}
{region && !timer.active && !showSettings && <PrayerTimesScreen prayers={prayers} />}
```
Add import at top:
```tsx
const showSettings = useStore((s) => s.showSettings);
```
**Commit:** `feat: settings toggle shows RegionSelect over prayer screen`

### Task 2.4 — Auto-start redesign (label + toggle, D2-A default)
**File:** `src/screens/PrayerTimes.tsx:84-86` — replace:
```tsx
<div className="prayer-autostart" onClick={toggleAutoStart} title="تشغيل عند بدء النظام">
  <span className="autostart-label">بدء تلقائي</span>
  <span className={`autostart-toggle ${autoStart ? 'on' : ''}`} />
</div>
```
**File:** `src/App.css:349-377` — replace block:
```css
.prayer-autostart {
  display: flex; align-items: center; gap: 0.5rem;
  justify-content: center; margin-bottom: 1rem;
  cursor: pointer; opacity: 0.5; transition: opacity 0.2s;
}
.prayer-autostart:hover { opacity: 0.9; }
.autostart-label { font-size: 0.8rem; color: #8b83a0; font-family: 'Noto Naskh Arabic', serif; }
.autostart-toggle {
  display: inline-block; width: 14px; height: 14px; border-radius: 50%;
  border: 1px solid rgba(232, 224, 208, 0.2); background: rgba(232, 224, 208, 0.08);
  transition: all 0.2s;
}
.autostart-toggle.on {
  background: #d4a843; border-color: #d4a843;
  box-shadow: 0 0 8px rgba(212, 168, 67, 0.4);
}
```
**Commit:** `feat: auto-start with visible label`

### Task 2.5 — Cleanup prayer-header position
**File:** `src/App.css:342-347` — remove `position: relative`:
```css
.prayer-header { text-align: center; margin-bottom: 0.25rem; margin-top: 1rem; }
```
**Commit:** `refactor: cleanup header position after auto-start move`

### Task 2.6 — Search result count
**File:** `src/screens/RegionSelect.tsx` — after search input:
```tsx
{search.trim() && (
  <div className="region-result-count">
    {filtered.length} {filtered.length === 1 ? 'مدينة' : 'مدن'}
  </div>
)}
```
**File:** `src/App.css` — add:
```css
.region-result-count {
  font-size: 0.75rem; color: #6b6380; text-align: right;
  margin-top: -0.3rem; padding-right: 0.3rem;
}
```
**Commit:** `feat: search result count badge`

### Task 2.7 — Loading spinner on "ابدأ"
**File:** `src/screens/RegionSelect.tsx`:
```tsx
const [starting, setStarting] = useState(false);
const handleStart = () => {
  if (starting) return; setStarting(true);
  // existing logic...
};
```
Button: `<button ... disabled={starting}>{starting ? <>جاري… <span className="spinner" /></> : 'ابدأ'}</button>`
**File:** `src/App.css` — add:
```css
.region-start-btn:disabled { opacity: 0.6; cursor: wait; }
.spinner {
  display: inline-block; width: 14px; height: 14px;
  border: 2px solid rgba(10, 14, 39, 0.2); border-top-color: #0a0e27;
  border-radius: 50%; animation: spin 0.6s linear infinite;
  vertical-align: middle; margin-right: 0.3rem;
}
@keyframes spin { to { transform: rotate(360deg); } }
```
**Commit:** `feat: loading spinner on start button`

### Task 2.8 — Auto-select first match on search
**File:** `src/screens/RegionSelect.tsx`:
```tsx
useEffect(() => {
  if (filtered.length === 1 && search.trim()) {
    const idx = CITIES.indexOf(filtered[0]);
    if (idx !== -1) setSelectedIdx(String(idx));
  }
}, [filtered, search]);
```
**Commit:** `feat: auto-select when search narrows to 1`

### Task 2.9 — Build + Phase 2 commit
`npm run build` → clean. `git add -A && git commit -m "phase 2: UX flow complete"`

### Task 2.10 — Manual UX verification
Click ⚙️ → RegionSelect appears. Toggle auto-start label. Search shows count. "ابدأ" spinner.

---

## Phase 3: 🎨 Design Tokens (8 tasks ~20 min)

> Corrected from V2: Linear-based spacing (4-8-16-24-32px scale), single 6px radius, `--color-overlay`

### Task 3.1 — Declare :root vars (at line ~48)
```css
:root {
  --color-bg: #0a0e27;
  --color-surface: rgba(232, 224, 208, 0.05);
  --color-overlay: rgba(17, 21, 51, 0.95);
  --color-text-primary: #e8e0d0;
  --color-text-secondary: #8b83a0;
  --color-text-muted: #a098c0;
  --color-text-verse: #6b6380;
  --color-accent: #d4a843;
  --color-accent-hover: #c49430;
  --color-accent-glow: rgba(212, 168, 67, 0.4);
  --color-border: rgba(232, 224, 208, 0.1);
  --color-placeholder: #7a7390;
  --color-toast-bg: rgba(17, 21, 51, 0.95);

  --space-2xs: 0.125rem;
  --space-xs: 0.25rem;   /* 4px */
  --space-sm: 0.5rem;    /* 8px */
  --space-md: 1rem;      /* 16px */
  --space-lg: 1.5rem;    /* 24px */
  --space-xl: 2rem;      /* 32px */

  --radius: 6px;
  --radius-lg: 12px;

  --font-arabic: 'Noto Naskh Arabic', serif;
  --font-latin: 'Inter', system-ui, sans-serif;

  --transition-fast: 0.15s;
  --transition-normal: 0.2s;
  --transition-slow: 0.3s;
}
```
**Commit:** `feat: CSS custom properties design tokens (Linear 4px scale)`

### Task 3.2 — Migrate background (#0a0e27 → var(--color-bg))
Replace in: body bg, .region-container bg, .prayer-container bg, .timer-container bg, .region-bg, .spinner border-top-color.
**Verify:** `grep -c '#0a0e27' src/App.css` → 1 (only :root declaration)
**Commit:** `refactor: bg → var(--color-bg)`

### Task 3.3 — Migrate text-primary (#e8e0d0 → var(--color-text-primary))
**Verify:** `grep '#e8e0d0' src/App.css | grep -v ':root'` → 0
**Commit:** `refactor: text-primary → var(--color-text-primary)`

### Task 3.4 — Migrate text-secondary (#8b83a0 → var(--color-text-secondary))
**Verify:** `grep '#8b83a0' src/App.css | grep -v ':root'` → 0
**Commit:** `refactor: text-secondary → var(--color-text-secondary)`

### Task 3.5 — Migrate accent + glow (#d4a843 → var(--color-accent))
Skip gradient defs. Keep `rgba(212,168,67,...)` in shadows as-is for now.
**Commit:** `refactor: accent → var(--color-accent)`

### Task 3.6 — Migrate border + placeholder
`rgba(232,224,208,0.1)` → `var(--color-border)`.
`#7a7390` → `var(--color-placeholder)`.
**Commit:** `refactor: border + placeholder → vars`

### Task 3.7 — Migrate spacing + radius
`padding: 2rem` → `var(--space-xl)`. `gap: 0.75rem` → `var(--space-sm)`.
`border-radius: 8px`|`6px` → `var(--radius)`.
`border-radius: 12px` → `var(--radius-lg)`.
**Commit:** `refactor: spacing + radius → vars`

### Task 3.8 — Migrate font-family
`'Noto Naskh Arabic', serif` → `var(--font-arabic)`.
`'Inter', ...` → `var(--font-latin)`.
**Verify:** `grep "Noto Naskh" src/App.css | grep -v var | grep -v @font-face` → 0.
**Commit:** `refactor: font-family → var(--font-arabic/latin)`

---

## Phase 4: ✨ Visual Polish (6 tasks ~1h)

### Task 4.1 — Countdown ring (with variant comment)
**File:** `src/screens/PrayerTimes.tsx` — add SVG ring in `.prayer-next-content`:
```tsx
const nextMs = Math.max(0, nextPrayer.time.getTime() - now);
const totalMs = 12 * 60 * 60 * 1000;
const progress = 1 - (nextMs / totalMs);
const circ = 2 * Math.PI * 20; const off = circ * (1 - progress);
// Variant B: text-only would skip SVG and show "45 د" as text
```
Add SVG inside `.prayer-next-content`. **File:** `src/App.css`:
```css
.next-ring { flex-shrink: 0; }
.next-ring-progress { transition: stroke-dashoffset 0.3s linear; }
```
**Commit:** `feat: countdown ring on next prayer card`

### Task 4.2 — Page transition
**File:** `src/App.css` — `.region-container`, `.prayer-container`, `.timer-content` already have fade animations ✅.
**File:** `src/App.tsx:92-94` — wrap in:
```tsx
<div style={{ opacity: showSettings ? 0 : 1, transition: 'opacity 0.2s' }}>
```
**Commit:** `feat: smooth opacity transition between screens`

### Task 4.3 — Prayer notification toast (no emoji)
**File:** `src/screens/PrayerTimes.tsx`:
```tsx
const [toast, setToast] = useState<{ name: string; time: string } | null>(null);
const prevIdx = useRef(nextIdx);
useEffect(() => {
  if (prevIdx.current !== nextIdx && nextIdx === 0) {
    setToast({ name: prayers[0].nameAr, time: formatTime12(prayers[0].time) });
    setTimeout(() => setToast(null), 4000);
  }
  prevIdx.current = nextIdx;
}, [nextIdx, prayers]);
// Render (before footer):
{toast && (
  <div className="prayer-toast">
    <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
      <path d="M10 2v1M10 17v1M2 10h1M17 10h1M4 4l1 1M15 15l1 1M4 16l1-1M15 5l1-1"
        stroke="#d4a843" strokeWidth="1.2" strokeLinecap="round"/>
    </svg>
    <div>
      <div className="prayer-toast-title">حان وقت صلاة {toast.name}</div>
      <div className="prayer-toast-time">{toast.time}</div>
    </div>
  </div>
)}
```
**File:** `src/App.css`:
```css
.prayer-toast {
  position: fixed; bottom: var(--space-xl); left: 50%; transform: translateX(-50%);
  display: flex; align-items: center; gap: var(--space-sm);
  background: var(--color-toast-bg); border: 1px solid rgba(212, 168, 67, 0.2);
  padding: var(--space-sm) var(--space-md); border-radius: 10px;
  animation: toastIn 0.3s ease; z-index: 300; backdrop-filter: blur(8px);
}
@keyframes toastIn {
  from { opacity: 0; transform: translateX(-50%) translateY(10px); }
  to { opacity: 1; transform: translateX(-50%) translateY(0); }
}
.prayer-toast-title { font-family: var(--font-arabic); font-size: 0.9rem; color: var(--color-accent); }
.prayer-toast-time { font-size: 0.75rem; color: var(--color-text-secondary); }
```
**Commit:** `feat: prayer toast with mosque-style icon`

### Task 4.4 — Verify frameless window
Already confirmed: title-bar drag region + no-drag on buttons ✅
`grep -r "app-region" electron/` → verify.
**Commit:** `chore: verify frameless drag regions`

### Task 4.5 — Scrollbar polish
**File:** `src/App.css`:
```css
::-webkit-scrollbar-thumb { background: rgba(232, 224, 208, 0.12); }
```
**Commit:** `style: brighter scrollbar thumb`

### Task 4.6 — Build + Phase 4 commit
`npm run build` → clean. `git add -A && git commit -m "phase 4: visual polish complete"`

---

## Phase 5: 📱 Responsive (3 tasks ~20 min)

> Note: prayerTimes uses `adhan` package — **no API**. Fully offline-native. No cache needed.
> Region already persisted via zustand/persist. So Phase 5 moves to responsive-only.

### Task 5.1 — Ultrawide breakpoint (1920+)
**File:** `src/App.css` — append:
```css
@media (min-width: 1920px) {
  .prayer-list { max-width: 400px; }
  .prayer-next-card { max-width: 400px; }
}
```

### Task 5.2 — Short screen compact mode
```css
@media (max-height: 600px) {
  .prayer-container { padding: 0.5rem 1rem; }
  .prayer-current-time { font-size: 2.5rem; }
  .prayer-next-card { margin-bottom: 0.75rem; padding: 0.6rem 1rem; }
  .prayer-row { padding: 0.4rem 0.75rem; }
}
```

### Task 5.3 — Build + Phase 5 commit
`npm run build` → clean. `git add src/App.css && git commit -m "phase 5: responsive breakpoints"`

---

## Phase 6: 🧪 DESIGN.md + QA (3 tasks ~25 min)

### Task 6.1 — DESIGN.md (Google spec)
**File:** `DESIGN.md` — full content (see appendix in plan file)
**Verify:** `npx @google/design-md lint DESIGN.md 2>/dev/null || echo "ok — lint tool optional"`

### Task 6.2 — Hardcoded color audit
```bash
grep -n '#[0-9a-f]\{6\}' src/App.css | grep -v 'var(--' | grep -v '/\*' | grep -v ':root'
# Expected: only @font-face src:url(...) and gradient colors
```

### Task 6.3 — Final QA + commit
```bash
npm run build
git add -A && git commit -m "phase 6: QA complete — Mawqūtan 10/10"
```

---

## 📊 V3 → V2 Changes

| Fix | V2 had | V3 has |
|-----|--------|--------|
| Routing | "if App.tsx" guess | ✅ App.tsx:92-94 verified |
| Cache | "API cache" (wrong) | ✅ Removed — adhan is offline |
| Spacing | md=0.75, lg=1.5, xl=2rem | ✅ md=1rem, lg=1.5rem, xl=2rem (Linear 4-8-16-24-32) |
| Radius | sm=6, md=8, lg=12px | ✅ Single 6px (Linear style) |
| Overlay token | missing | ✅ `--color-overlay` for toast |
| Task granularity | 32 tasks, some large | ✅ 36 tasks, all 2-5 min |
| Excalidraw | not mentioned | ✅ Step 0: wireframe before coding |
| Toast icon | 🕌 emoji | ✅ SVG pattern |

---

## ✅ Ready

Say **"Excalidraw"** أولاً باش نرسم الـ wireframe ونشوفو الخيارات قبل التنفيذ. أو **"execute"** باش نبداو مباشرة من Task 1.1.
